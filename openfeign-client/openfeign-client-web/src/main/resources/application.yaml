server:
  port: 8081
  servlet:
    context-path: /demo
spring:
  jackson:
    default-property-inclusion:  non_null
    date-format:  yyyy-MM-dd HH:mm:ss
feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: basic
